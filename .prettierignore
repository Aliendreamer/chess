# Generated API artifacts — committed but never hand-formatted (keeps gen:api:check stable).
apps/backend/openapi/openapi.json
apps/frontend/src/lib/api/schema.d.ts

# Backend appsettings — sensitive/secret-bearing config; never reformat (agent-off-limits).
apps/backend/Config/

# Generated lockfile.
pnpm-lock.yaml

# Store-installed skills (setup-flow re-fetches them); not authored here.
.claude/skills/

# Plugin-installed slash commands (OpenSpec); not authored here either.
.claude/commands/opsx/

# Harness-managed: Claude Code rewrites this whenever the sandbox is toggled, and the sandbox makes
# it read-only, so `prettier --write` cannot fix it and the pre-commit hook fails on it.
.claude/settings.json

# Serena project config + memories (Serena-owned formatting).
.serena/

# Sandbox masks: inside the Claude Code sandbox these appear as /dev/null devices and error on read.
.mcp.json
.claude/loop.md
.claude/launch.json
.claude/scheduled_tasks.json
.claude/settings.local.json

# generated route tree + app build output
apps/frontend/src/routeTree.gen.ts
.output/

# Playwright run artifacts (gitignored, but prettier does not read .gitignore).
test-results/
playwright-report/
