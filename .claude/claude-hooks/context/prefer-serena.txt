Prefer semantic code tools for CODE search — Serena's search_for_pattern / find_symbol /
find_referencing_symbols, or the editor LSP. Raw grep/rg is the right tool for configs, compose
files, logs and other non-code; use it there without apology. Shown once per session.
